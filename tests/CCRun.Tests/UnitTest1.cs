namespace CCRun.Tests;

public class ScaffoldTests
{
    // Placeholder sanity test so `dotnet test` is green — the Milestone M0
    // "CI build passing" exit criterion. Real tests arrive in Phase 1.
    [Fact]
    public void Scaffold_IsWiredUp()
    {
        Assert.True(true);
    }
}
